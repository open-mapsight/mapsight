---
"@mapsight/ui": minor
"@mapsight/lib-js": patch
---

Restore legacy embed bag helpers and host entry aliases (createEmbedBag, default plugins / hook sugar, filterFeatures, withKeyboard, global-event-hub, is-numeric) so pre-OSS CMS hosts can migrate with smaller diffs.
