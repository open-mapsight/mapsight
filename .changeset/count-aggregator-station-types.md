---
"@mapsight/count-aggregator-api": minor
---

Sync the count-aggregator OpenAPI contract with current platform station types and ignore unknown types in `GET /station-types` instead of failing the whole list.
