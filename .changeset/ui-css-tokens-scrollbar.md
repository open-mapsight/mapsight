---
"@mapsight/ui": minor
---

Emit `--ms3-*` design tokens from configured Sass variables (mixin + `$ms3-tokens-selector`), use them for easy color/size/z-index values, and add minimal scrollbar utilities
