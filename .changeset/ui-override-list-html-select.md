---
"@mapsight/ui": patch
---

Restore select/deselect interaction props on FeatureListItem when `overrideListHtml` short-circuits past FeatureSelectButton, so host list rows (e.g. VMZ Baustellen) open feature details on click again.
