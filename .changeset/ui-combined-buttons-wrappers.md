---
"@mapsight/ui": patch
---

Restore `.ms3-map-overlay-combined-buttons__button` wrappers around CombinedButtons children so host CSS can share a single 1px edge between zoom +/- (and similar stacked overlay controls).
