---
"@mapsight/ui": minor
---

Add optional geolocation distance labels for feature list items. Export `DistanceLabel`, wire via `itemDistanceLabelAs` / `distanceLabelAs` (default off), and style opted-in rows with `ms3-list__main--with-distance` in the 2022-03 theme.
