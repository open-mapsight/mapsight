---
"@mapsight/traffic-style": minor
---

Add icon metadata helpers, fix variant fallback, align composed default pictogram icons with the legacy bitmap footprint, add a `traffic-copy-assets` CLI for filtered deploy copies, and keep the published icon catalog split to PNG/SVG-only directories.
