import type {CountAggregatorTheme} from "../lib/theme.js";

/** Default theme tokens aligned with package CSS variables in `styles.css`. */
export const smartCityCountAggregatorTheme: CountAggregatorTheme = {
	colors: {
		primary: "#2563eb",
		primaryHover: "#1d4ed8",
		border: "#e5e7eb",
		mutedForeground: "#6b7280",
	},
};
